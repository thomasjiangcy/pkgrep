version: 1
name: pkgrep-usage
display_name: pkgrep Usage
short_description: Fetch and link dependency source with pkgrep
default_prompt: Use pkgrep commands to fetch dependency source code. Treat linked `.pkgrep` paths as internal details, and report relevant dependency code back to the user with inline snippets or summaries instead of path-only references.
