pre-commit:
  parallel: true
  commands:
    no_mocks:
      run: ./.dev/check_no_mocks.sh
    fmt_check:
      run: cargo fmt --all --check

pre-push:
  commands:
    ci:
      run: mise exec -- just ci
